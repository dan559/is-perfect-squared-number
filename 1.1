#include <cmath>
bool is_square(int n)
{
  double checkIfSquare = sqrt(n);
  return ((checkIfSquare - floor(checkIfSquare))==0);
}
int main()
{
int n;
cout<< "Enter number to check." << endl;
cin >> n;



return 0;
}
